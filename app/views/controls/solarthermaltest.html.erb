<h1>Solar Thermal Test</h1

<p>
This test sends the following to mServer (where <%= DEVICES["solar_thermal"] %> is the Device ID of the Solar Thermal Datalogger).<br />
This tests the GET command:
</p>
<ul>
<li>SendCommand.new(<%= DEVICES["solar_thermal"] %>, "GET")</li>
</ul>
<p>This commands can be modified in the <em>solarthermaltest</em> method in <strong>app/controllers/controls_controller.rb</strong>.</p>
<p>This HTML output can be modified in <strong>app/views/controls/solarthermaltest.html.erb</strong>.</p>
<p>The results of the commands can be monitored in the <strong>mServer Log</strong> in the <strong>mControl Service Manager</strong>.</p>
<p>Changes can be made 'on-the-fly' and only require a page refresh - not a server restart.</p>

<h2>Results</h2>
<%= @stdl.inspect %>